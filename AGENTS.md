# Agent notes

When working in this repository, follow the skill at:

`skills/university-partner-outreach/SKILL.md`

The same folder is linked from `.agents/skills/`, `.claude/skills/`, and `.cursor/skills/` for tool auto-discovery. Read files under `references/` (`niche-roles.md`, `sources.md`, `email-accuracy.md`, `examples.md`) as needed; executables live under `scripts/`. Example people and emails in docs are fictional.

Do not commit real outreach CSVs with personal data unless explicitly asked.
